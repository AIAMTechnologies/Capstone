# Local environment variables for the Lead Allocation backend.
# Populate these values with your Supabase credentials before running the app.

# PostgreSQL connection string. For Supabase, use the "Connection string" value
# from the dashboard (typically ends with `supabase.co`).
SUPABASE_DB_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Optional explicit SSL mode. Supabase URLs usually specify `require`, so you can leave
# this empty unless you need a different value.
# DB_SSLMODE="require"

# Supabase API keys (Project Settings -> API)
SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_SECRET_KEY=""

# Application secret used for signing JWT tokens. Generate a strong random value
# for production deployments.
SECRET_KEY="change-me"

# Optional: override the path to the trained lead model artifact.
# LEAD_MODEL_PATH="backend/model_artifacts/lead_scoring_pipeline.pkl"

# Optional: API key for the configured geocoding provider (Google Maps, etc.).
# GEOCODING_API_KEY=""
# GEOCODING_PROVIDER="nominatim"
